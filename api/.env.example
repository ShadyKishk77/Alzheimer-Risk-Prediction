# Alzheimer's Risk Prediction API - Configuration
# Copy this file to .env and customize as needed

# ==================== API Configuration ====================

# Server settings
HOST=0.0.0.0
PORT=8000
RELOAD=false
LOG_LEVEL=info

# ==================== Security ====================

# CORS Origins (comma-separated URLs)
# For production, restrict to your actual domains
CORS_ORIGINS=["*"]
# Example production setting:
# CORS_ORIGINS=["https://yourdomain.com", "https://app.yourdomain.com"]

# Request size limit (bytes)
MAX_REQUEST_SIZE=1048576

# ==================== Model Configuration ====================

# Model paths (optional - defaults are auto-detected)
# Uncomment and modify if using custom model locations
# MODEL_PATH=/path/to/custom/model.pkl
# SCALER_PATH=/path/to/custom/scaler.pkl
# FEATURES_PATH=/path/to/custom/feature_names.pkl

# Model metadata
MODEL_VERSION=1.0.0
MODEL_NAME=Gradient Boosting Classifier
EXPECTED_FEATURES=32

# ==================== API Metadata ====================

API_TITLE=Alzheimer's Disease Risk Prediction API
API_VERSION=1.0.0
API_DESCRIPTION=Production-ready REST API for predicting Alzheimer's disease risk using machine learning

# ==================== Advanced Settings ====================

# Uvicorn workers (production only - do not use with --reload)
# WORKERS=4

# Log format
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# ==================== Notes ====================

# - Copy this file to .env in the api/ directory
# - Settings are loaded automatically by pydantic-settings
# - Environment variables override .env file values
# - Never commit .env file to version control (add to .gitignore)
# - See api/README.md for full configuration documentation
